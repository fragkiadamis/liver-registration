{
  "description": "Register a pair of images using the elastix CLI.",
  "arguments": {
    "required": {
      "-i": "Path to the nifty files.",
      "-o": "Path to the registration output.",
      "-p": "Pipeline json file. Specify the registration pipeline file."
    },
    "optional": {
      "-fm": "Specify fixed modality e.g. -fm SPECT-CT. Default is SPECT-CT.",
      "-mm": "Specify moving modality e.g. -mm ceMRI. Default is ceMRI."
    }
  }
}