{
  "description": "Create a database where the processed nifty images are stored.",
  "arguments": {
    "required": {
      "-i": "Path to the generated masks.",
      "-o": "Path to the directory where the processed masks are going to be saved.",
      "-t": "Post processing for registration (-t reg) or segmentation (-t seg).",
      "-ref": "Path of reference directory for resampling."
    },
    "optional": {
    }
  }
}