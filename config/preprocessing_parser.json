{
  "description": "Create a database where the processed nifty images are stored.",
  "arguments": {
    "required": {
      "-i": "Path to the directory where the nifty database exists.",
      "-o": "Path to the directory where the processed database is going to be saved.",
      "-t": "The preprocessing type [elx: for elastix registration, dls: for ct deep learning segmentation, dlr: for deep learning registration].",
    },
    "optional": {
      "--prealigned": "If the grobal aligned MRIs are going to be preprocessed or the originals [1, 0].",
      "-d": "The base dir of the prealligned mri."
    }
  }
}